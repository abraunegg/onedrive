@startuml
start
note left: Operational Mode 'onedrive --sync'
:Query OneDrive /delta API for online changes;
note left: Online change data is authoritative\nfor the remote item being reconciled
:Process received JSON data;
if (JSON item is a changed file?) then (yes)
  :Queue/download replacement to <canonical>.partial;
  :Validate HTTP result, size and file hash;
  if (Does canonical file exist locally?) then (yes)
    :Read current canonical hash;
    :Read last in-sync DB baseline when available;
    if (Canonical matches incoming online content?) then (yes)
      :No safeBackup required;
    else (no)
      if (Canonical matches last in-sync DB baseline?) then (yes)
        :No independent local change;\nno safeBackup required;
      else (no)
        :Create and verify safeBackup copy;
        note left: Canonical pathname remains present\nuntil replacement commit
      endif
    endif
    :Recheck canonical did not change during commit preparation;
  else (no)
  endif
  :Atomically promote validated .partial to canonical pathname;
else (no)
  :Other handling for directories | root objects | deleted items;
endif
:Perform database consistency and integrity check;
:Scan local filesystem for new data to upload;
note left: A newly-created safeBackup may be uploaded here\nunless excluded by skip_file
stop
@enduml
